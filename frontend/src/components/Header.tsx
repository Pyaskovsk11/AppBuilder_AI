import React from "react";

const Header: React.FC = () => (
  <header className="h-16 flex items-center px-6 border-b border-gray-200 dark:border-neutral-800 bg-white dark:bg-neutral-900 shadow-sm">
    <span className="font-bold text-xl tracking-tight text-gray-900 dark:text-white">AppBuilder AI</span>
  </header>
);

export default Header;
